ASMCMP_BIN := $(BIN_DIR)/plcmp

ASMCMP_INC_DIR := $(ASMCMP_DIR)/inc
ASMCMP_SRC_DIR := $(ASMCMP_DIR)/src
ASMCMP_OBJ_DIR := $(ASMCMP_DIR)/obj
ASMCMP_DEP_DIR := $(ASMCMP_DIR)/obj

# PL COMPILER INCLUDES
ASMCMP_INCLUDES := $(ASMCMP_INC_DIR)
ASMCMP_INCLUDES += 

# PL COMPILER DEFINITIONS
ASMCMP_DEFINITIONS := 
ASMCMP_DEFINITIONS += 

# PL COMPILER SOURCES
ASMCMP_SRCS := $(wildcard $(ASMCMP_SRC_DIR)/*.c)
ASMCMP_SRCS_NOTDIR := $(notdir $(ASMCMP_SRCS))

# PL COMPILER OBJECTS
ASMCMP_OBJS := $(patsubst %.c, $(ASMCMP_OBJ_DIR)/%.o, $(ASMCMP_SRCS_NOTDIR))
ASMCMP_OBJS_NOTDIR := $(notdir $(ASMCMP_OBJS))

# PL COMPILER DEPENDENCIES
ASMCMP_DEPS := $(patsubst %.o, $(ASMCMP_DEP_DIR)/%.d, $(ASMCMP_OBJS_NOTDIR))
ASMCMP_DEPS_NOTDIR := $(notdir $(ASMCMP_DEPS))

$(ASMCMP_OBJ_DIR)/%.o: $(ASMCMP_SRC_DIR)/%.c
	$(CC) -c $< $(FLAGS) -I $(ASMCMP_INCLUDES) -MMD -o $@

-include $(ASMCMP_DEPS)